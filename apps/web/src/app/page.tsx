/**
 * Placeholder index route.
 *
 * Deliberately not a product screen: it renders the process identity only, so
 * that `apps/web` is a real bootable Next.js process without anticipating the
 * Portal, Studio or Control surfaces (P15+) or the design system (P18).
 */
export default function Page() {
  return <main>ZENLABS web process. No product surface is mounted yet.</main>;
}
