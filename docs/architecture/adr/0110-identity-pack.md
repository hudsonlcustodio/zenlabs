# ADR-0110 — IdentityPack as provider-independent identity asset

**Status:** Accepted in principle

## Decision
Visual identity is represented by versioned ZENLABS-owned source/provenance/reference assets. Provider clones/IDs are bindings only.

Consent and revocation guard every identity-bearing operation.
