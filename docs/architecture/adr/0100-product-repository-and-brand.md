# ADR-0100 — ZENLABS repository, brand and legacy boundary

**Status:** Accepted

## Decision
The canonical product is `ZENLABS | Laboratório de Clones` in repository `hudsonlcustodio/zenlabs`.

The VYRA ZIP is a migration seed only. Its documentation is archived under `docs/_legacy/` and is never canonical.

Technical namespace: `@zenlabs/*`.
