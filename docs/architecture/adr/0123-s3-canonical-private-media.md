# ADR-0123 — S3 private as canonical media storage

**Status:** Proposed

## Decision
ZENLABS-owned media is stored in private Amazon S3 with encryption at rest.

Provider result URLs are temporary transport references only.

Presigned access is short-lived and policy controlled.
