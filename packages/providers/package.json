{
  "name": "@zenlabs/providers",
  "version": "0.0.0",
  "private": true,
  "description": "Provider ports + adapters + capability registry + error taxonomy.",
  "license": "UNLICENSED",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit -p tsconfig.json"
  },
  "zenlabs": {
    "kind": "package",
    "architectureRef": "architecture.md §2.2",
    "allowedDependencies": [
      "@zenlabs/contracts"
    ]
  },
  "dependencies": {
    "@zenlabs/contracts": "workspace:*"
  }
}
